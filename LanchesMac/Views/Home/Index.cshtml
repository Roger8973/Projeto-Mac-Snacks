@model HomeViewModel

<partial name="_Carousel" />

<h2>Os preferidos da semana </h2>
<h4>Bateu aquela fome? Bora pedir!</h4>

<div class="row">
    @foreach (var snack in Model.FavoriteSnacks)
    {
        <partial name="_SnacksSummary" for="@snack"/>
    }
</div>